# LLM API Keys (at least one required)
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AI...

# MCP Server URL
MCP_SERVER_URL=https://requirements-advisor-production.up.railway.app/mcp

# Database URL
# For local development (SQLite):
DATABASE_URL=sqlite+aiosqlite:///./data/sessions.db
# For production (PostgreSQL):
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/database

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
LOG_LEVEL=INFO
LOG_JSON=false

# LLM Settings
# Maximum tool-calling iterations per request (increase for complex queries)
LLM_MAX_ITERATIONS=10

# Frontend Configuration
API_URL=http://localhost:8000
